# Not needed to run this project locally -- everything runs against local
# files and a local DuckDB file. Kept here in case of a future cloud port
# (see docs/architecture.md).

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
